# Asterisk.ARI

No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `asterisk_ari` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [{:asterisk_ari, "~> 0.1.0"}]
end
```

Documentation can be generated with [ExDoc](https://github.com/elixir-lang/ex_doc)
and published on [HexDocs](https://hexdocs.pm). Once published, the docs can
be found at [https://hexdocs.pm/asterisk_ari](https://hexdocs.pm/asterisk_ari).

# Generate

```
docker run --rm -v ${PWD}:/local -w '/local' swaggerapi/swagger-codegen-cli generate \
  -i http://192.168.0.240:8088/ari/api-docs/resources.json \
  -l elixir \
  -o /local/ \
  -a "Authorization: Basic YXN0ZXJpc2s6YXN0ZXJpc2s=" \
  -c ./config.json
```
